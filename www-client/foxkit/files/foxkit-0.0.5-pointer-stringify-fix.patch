diff -ur a/src/cookiemonster.cpp b/src/cookiemonster.cpp
--- a/src/cookiemonster.cpp	2008-07-29 13:21:56.000000000 -0500
+++ b/src/cookiemonster.cpp	2008-10-16 22:39:34.000000000 -0500
@@ -27,8 +27,8 @@
 
 cookieMonster::cookieMonster(QObject * parent) : QNetworkCookieJar(parent), s_cookieSaveInterval(3000), m_readOnly(false)
 {
-        setObjectName("cookieMonster:" + QString::number(uint(this)));
-        std::cout << "CREATE:cookieMonster," << uint(this) << ";parent:" << uint(parent) << std::endl;
+        setObjectName("cookieMonster:" + QString().sprintf("%p", this));
+        std::cout << QString().sprintf("CREATE:cookieMonster,%p;parent:%p", this, parent).toLocal8Bit().constData() << std::endl;
         loadFromDisk();
         m_timerId = startTimer(s_cookieSaveInterval); //Auto-save cookies after changes
 }
@@ -37,7 +37,7 @@
 cookieMonster::~cookieMonster()
 {
         saveIfTainted();
-        std::cout << "DESTROY:cookieMonster," << uint(this) << ";parent:" << uint(parent()) << std::endl;
+        std::cout << QString().sprintf("DESTROY:cookieMonster,%p;parent:%p", this, parent()).toLocal8Bit().constData() << std::endl;
 }
 
 void cookieMonster::timerEvent(QTimerEvent * event)
diff -ur a/src/foxkit.cpp b/src/foxkit.cpp
--- a/src/foxkit.cpp	2008-07-29 13:21:56.000000000 -0500
+++ b/src/foxkit.cpp	2008-10-16 22:40:11.000000000 -0500
@@ -43,8 +43,8 @@
 
 foxkit::foxkit(NetManager *net) : KXmlGuiWindow(0), tabs(this), m_progress(this), m_addressBar(this)
 {
-        setObjectName("foxkit:" + QString::number(uint(this)));
-        std::cout << "CREATE:foxkit," << uint(this) << ";parent:" << uint(parent()) << std::endl;
+        setObjectName("foxkit:" + QString().sprintf("%p", this));
+        std::cout << QString().sprintf("CREATE:foxkit,%p;parent:%p", this, parent()).toLocal8Bit().constData() << std::endl;
         setWindowTitle(i18n("Foxkit Web Browser"));
         netman = net;
         cookies = new cookieViewer(qobject_cast<cookieMonster*>(netman->cookieJar()));
@@ -74,7 +74,7 @@
 
 foxkit::~foxkit()
 {
-        std::cout << "DESTROY:foxkit," << uint(this) << ";parent:" << uint(parent()) << std::endl;
+        std::cout << QString().sprintf("DESTROY:foxkit,%p;parent:%p", this, parent()).toLocal8Bit().constData() << std::endl;
         cookies->close();
         cookies->deleteLater();
 }
diff -ur a/src/netmanager.cpp b/src/netmanager.cpp
--- a/src/netmanager.cpp	2008-07-29 13:21:56.000000000 -0500
+++ b/src/netmanager.cpp	2008-10-16 22:42:03.000000000 -0500
@@ -26,8 +26,8 @@
 
 NetManager::NetManager(QObject * parent) : QNetworkAccessManager(parent), jar(this)
 {
-        setObjectName("NetManager:" + QString::number(uint(this)));
-        std::cout << "CREATE:NetManager," << uint(this) << ";parent:" << uint(parent) << std::endl;
+        setObjectName("NetManager:" + QString().sprintf("%p", this));
+        std::cout << QString().sprintf("CREATE:NetManager,%p;parent:%p", this, parent).toLocal8Bit().constData() << std::endl;
         setCookieJar(&jar);
         connect(this, SIGNAL(sslErrors(QNetworkReply*, const QList< QSslError > &)), this, SLOT(notifyOnSslError(QNetworkReply*, const QList< QSslError > &)));
 }
@@ -35,7 +35,7 @@
 
 NetManager::~NetManager()
 {
-        std::cout << "DESTROY:NetManager," << uint(this) << ";parent:" << uint(parent()) << std::endl;
+        std::cout << QString().sprintf("DESTROY:NetManager,%p;parent:%p", this, parent()).toLocal8Bit().constData() << std::endl;
 }
 
 /*QNetworkReply* NetManager::createRequest ( Operation op, const QNetworkRequest & req, QIODevice * outgoingData = 0 )
diff -ur a/src/tools.cpp b/src/tools.cpp
--- a/src/tools.cpp	2008-07-29 13:21:56.000000000 -0500
+++ b/src/tools.cpp	2008-10-16 22:38:33.000000000 -0500
@@ -26,8 +26,8 @@
 cookieViewer::cookieViewer(cookieMonster *jar)
  : QTableWidget(0)
 {
-        setObjectName("cookieViewer:" + QString::number(uint(this)));
-        std::cout << "CREATE:cookieViewer," << uint(this) << ";parent:" << uint(parent()) << std::endl;
+        setObjectName("cookieViewer:" + QString().sprintf("%p", this));
+        std::cout << QString().sprintf("CREATE:cookieViewer,%p;parent:%p", this, parent()).toLocal8Bit().constData() << std::endl;
         setWindowTitle("Cookie List, F5 for refresh");
         m_refresh = new QAction("Refresh Cookies", this);
         m_refresh->setShortcut(Qt::Key_F5);
@@ -44,7 +44,7 @@
 
 cookieViewer::~cookieViewer()
 {
-        std::cout << "DESTROY:cookieViewer," << uint(this) << ";parent:" << uint(parent()) << std::endl;
+        std::cout << QString().sprintf("DESTROY:cookieViewer,%p;parent:%p", this, parent()).toLocal8Bit().constData() << std::endl;
         delete m_refresh;
 }
 
diff -ur a/src/webpage.cpp b/src/webpage.cpp
--- a/src/webpage.cpp	2008-07-29 13:21:56.000000000 -0500
+++ b/src/webpage.cpp	2008-10-16 22:41:41.000000000 -0500
@@ -25,14 +25,14 @@
 
 webPage::webPage(QObject* parent) : QWebPage(parent), m_mouseButtons(Qt::NoButton), m_kbdMods(Qt::NoModifier)
 {
-        setObjectName("webPage:" + QString::number(uint(this)));
-        std::cout << "CREATE:webPage," << uint(this) << ";parent:" << uint(parent) << std::endl;
+        setObjectName("webPage:" + QString().sprintf("%p", this));
+        std::cout << QString().sprintf("CREATE:webPage,%p;parent:%p", this, parent).toLocal8Bit().constData() << std::endl;
 }
 
 
 webPage::~webPage()
 {
-        std::cout << "DESTROY:webPage," << uint(this) << ";parent:" << uint(parent()) << std::endl;
+        std::cout << QString().sprintf("DESTROY:webPage,%p;parent:%p", this, parent()).toLocal8Bit().constData() << std::endl;
 }
 
 bool webPage::acceptNavigationRequest(QWebFrame *frame, const QNetworkRequest &request, NavigationType type)
diff -ur a/src/webtab.cpp b/src/webtab.cpp
--- a/src/webtab.cpp	2008-07-29 13:21:56.000000000 -0500
+++ b/src/webtab.cpp	2008-10-16 22:38:33.000000000 -0500
@@ -23,7 +23,7 @@
         //tabs very quickly causes a crash.
 webTab::webTab(QWidget* parent): QTabWidget(parent)
 {
-        setObjectName("webTab:" + QString::number(uint(this)));
+        setObjectName("webTab:" + QString().sprintf("%p", this));
         tabs = new QTabBar(this);
         setTabBar(tabs);
 }
diff -ur a/src/webview.cpp b/src/webview.cpp
--- a/src/webview.cpp	2008-07-29 13:21:56.000000000 -0500
+++ b/src/webview.cpp	2008-10-16 22:38:33.000000000 -0500
@@ -28,8 +28,8 @@
 webView::webView(QWidget* parent) : QWebView(parent), m_progress(0), m_urlWanted("")
 {
         m_page = new webPage(this);
-        setObjectName("webView:" + QString::number(uint(this)));
-        std::cout << "CREATE:webView," << uint(this) << ";parent:" << uint(parent) << std::endl;
+        setObjectName("webView:" + QString().sprintf("%p", this));
+        std::cout << QString().sprintf("CREATE:webView,%p;parent:%p", this, parent).toLocal8Bit().constData() << std::endl;
         setPage(m_page);
         connect(this, SIGNAL(urlChanged(QUrl)),
                 this, SLOT(urlCatcher(QUrl)));
@@ -49,7 +49,7 @@
 
 webView::~webView()
 {
-        std::cout << "DESTROY:webView," << uint(this) << ";parent:" << uint(parent()) << std::endl;
+        std::cout << QString().sprintf("DESTROY:webView,%p;parent:%p", this, parent()).toLocal8Bit().constData() << std::endl;
         m_page->disconnect();
 }
 
diff -ur a/src/wrapper.cpp b/src/wrapper.cpp
--- a/src/wrapper.cpp	2008-07-29 13:21:56.000000000 -0500
+++ b/src/wrapper.cpp	2008-10-16 22:40:50.000000000 -0500
@@ -41,7 +41,7 @@
 */
 wrapper::wrapper() : QObject(), netman(this)
 {
-        setObjectName("wrapper:" + QString::number(uint(this)));
+        setObjectName("wrapper:" + QString().sprintf("%p", this));
         connect(kapp, SIGNAL(lastWindowClosed()),
             this, SLOT(cleanUpAndQuit()));
         QString iconPath = KStandardDirs::locateLocal("cache", "foxkit");
@@ -58,14 +58,14 @@
         if(!iconFailure)
                 QWebSettings::globalSettings()->setIconDatabasePath(iconPath);
 
-        std::cout << "CREATE:wrapper," << uint(this) << ";parent:" << uint(parent()) << std::endl;
+        std::cout << QString().sprintf("CREATE:wrapper,%p;parent:%p", this, parent()).toLocal8Bit().constData() << std::endl;
         newFoxkit(QNetworkRequest(foxkit::getHome()));
 }
 
 
 wrapper::~wrapper()
 {
-        std::cout << "DESTROY:wrapper," << uint(this) << ";parent:" << uint(parent()) << std::endl;
+        std::cout << QString().sprintf("DESTROY:wrapper,%p;parent:%p", this, parent()).toLocal8Bit().constData() << std::endl;
         std::cout << std::flush;
 }
 
