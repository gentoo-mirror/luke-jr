--- a/2.6.22/4200_fbsplash-0.9.2-r5.patch
+++ b/2.6.22/4200_fbsplash-0.9.2-r5.patch
@@ -1769,10 +1769,10 @@
  	KERN_MAX_LOCK_DEPTH=74,
  	KERN_NMI_WATCHDOG=75, /* int: enable/disable nmi watchdog */
  	KERN_PANIC_ON_NMI=76, /* int: whether we will panic on an unrecovered */
 +	KERN_FBSPLASH=77,	/* string: path to fbsplash helper */
- };
- 
- 
+ 	KERN_SILENCE_LEVEL=200, /* int: Console silence loglevel */
+ 	KERN_ALLOC_FAIL_WARN=201, /* int: whether we'll print "alloc failure" */
+ 	KERN_VIRT_OSRELEASE=205,/* virtualization of utsname.release */
 --- a/kernel/sysctl.c
 +++ b/kernel/sysctl.c
 @@ -89,6 +89,9 @@ static int ngroups_max = NGROUPS_MAX;
--- a/2.6.22/4205_vesafb-tng-1.0-rc2.patch
+++ b/2.6.22/4205_vesafb-tng-1.0-rc2.patch
@@ -433,9 +433,9 @@
 @@ -250,7 +250,7 @@ static void send_sig_all(int sig)
  	struct task_struct *p;
  
- 	for_each_process(p) {
--		if (p->mm && !is_init(p))
-+		if (p->mm && !is_init(p) && !(p->flags & PF_BORROWED_MM))
+ 	for_each_process_all(p) {
+-		if (p->mm && !is_global_init(p))
++		if (p->mm && !is_global_init(p) && !(p->flags & PF_BORROWED_MM))
  			/* Not swapper, init nor kernel thread */
  			force_sig(sig, p);
  	}
--- a/2.6.22/4405_alpha-sysctl-uac.patch
+++ b/2.6.22/4405_alpha-sysctl-uac.patch
@@ -137,9 +137,9 @@
 +#ifdef CONFIG_ALPHA_UAC_SYSCTL
 +	KERN_UAC_POLICY=78,	/* int: Alpha unaligned access control policy flags */
 +#endif /* CONFIG_ALPHA_UAC_SYSCTL */
- };
- 
- 
+ 	KERN_SILENCE_LEVEL=200, /* int: Console silence loglevel */
+ 	KERN_ALLOC_FAIL_WARN=201, /* int: whether we'll print "alloc failure" */
+ 	KERN_VIRT_OSRELEASE=205,/* virtualization of utsname.release */
 @@ -259,6 +262,17 @@ enum
  	PTY_NR=2
  };
